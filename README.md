# HealthPlate AI — Frontend Scaffold

This project is a Vite + React scaffold for the HealthPlate AI marketing site.

Quick start

1. Install dependencies:

```bash
npm install
```

2. Run dev server:

```bash
npm run dev
```

This scaffold includes TailwindCSS, Framer Motion, Lucide icons and other UI helpers.
